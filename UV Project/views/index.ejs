<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apply Sunscreen?</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/style.css">
</head>
<body class = "justify-content-center align-items-center d-flex vh-100">
    <div class = "container text-center border rounded-3 customContainer ">
        <h2>Do you need to apply sunscreen today?</h2>
        <form action = "/submit" class = "form" method="post">
            <input type="text" name = "country" placeholder="Enter Country Name" class = "form-control my-3 w-50 mx-auto" required>
            <input type="text" name = "pin" placeholder="PIN Code" class = "form-control my-3 w-50 mx-auto" required>
            <button type="submit" class = "btn btn-primary mb-3 customColor">Submit</button>
        </form>
        <% if(locals.data) { %>
            <% if(data.uv_max>3){%>
                <h3 class = "text-danger">Yes, you should apply sunscreen today! (UV Index: <%= data.uv_max %>)</h3>
            <% } else { %>
                <h3 class = "text-success">No, you don't need to apply sunscreen today! (UV Index: <%= data.uv_max %>)</h3>
            <% } %>
        <% } else {%>
            <h3>Please enter the details to check UV index.</h3>
            <% if(locals.content) { %>
                <p><%= content %></p>
            <%}%>
        <% } %>
    </div>
</body>
</html>