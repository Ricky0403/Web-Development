<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
    <link href="styles/style.css" rel = 'stylesheet'/>
    <title>Daily Blog</title>
</head>
<body class = "d-flex flex-column min-vh-100">
    <%-include("partial/header.ejs")%>
    <h1 class="about-title">Your Blogs</h1>
    <%if (locals.title && locals.title.length > 0) { %>
        <div class="d-flex flex-wrap gap-4 m-4">
            <% for (let i = 0; i < title.length; i++) { %>
                <a href="/blog/<%= i %>" class="text-decoration-none">
                    <div class="card" style="width: 18rem;">
                        <img src="/uploads/<%= images[i] %>" class="card-img-top border-bottom" alt="..." style="height: 200px; object-fit: cover;">
                        <div class="card-body">
                            <h5 class="card-title"><%= title[i] %></h5>
                        </div>
                    </div>
                </a>
            <% } %>
        </div>
    <%} else { %>
        <div class="m-5 about-content">
            <p>No blogs to show. Create a new blog post!</p>
        </div>
    <% } %>
     
    <%-include("partial/footer.ejs")%>
</body>
</html>